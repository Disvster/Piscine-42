                       //0 se tiver '+'||'-'|| whitespaces; 
	if (b_len <= 1 || !verify(base))
		return (NULL);
	if	

char	*ft_convert_base(char *nbr, char *base_from, char *base_to)
{
	ft_atoi_base(nbr, base_from); //return (int res)
	len_nbr_pralloc = '?'; //vai ter que ser calculado por outra func  
	ft_putnbr_base(atoi_res, len_nbr_pralloc, base_to);
	return (nbr);
}
